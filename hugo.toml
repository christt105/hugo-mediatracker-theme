# Theme module configuration. Importing this theme transitively pulls in
# hugo-blog-awesome, which it builds on top of.
[module]
  [[module.imports]]
    path = "github.com/hugo-sid/hugo-blog-awesome"
